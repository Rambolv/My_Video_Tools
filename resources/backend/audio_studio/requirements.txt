# ============================================
# 声音自由生成修改大师 — 依赖清单
# ============================================
# 安装: pip install -r requirements.txt -i https://mirrors.tuna.tsinghua.edu.cn/pypi/web/simple

# ─── 核心依赖 ───
torch>=2.0.0
torchaudio>=2.0.0
soundfile>=0.12
numpy>=1.24
scipy>=1.10

# ─── 音频工具 ───
pydub>=0.25
librosa>=0.10
ffmpeg-python>=0.2

# ─── WebUI ───
gradio>=4.0,<6.0

# ─── API ───
fastapi>=0.100
uvicorn>=0.20
pydantic>=2.0

# ─── 增强能力（可选） ───
# demucs>=4.0          # 人声伴奏分离
# voice-sculptor       # 音色指令编辑
# gpt-sovits           # 定制化LoRA训练
# song-bloom           # 长歌曲续写

# ─── VoxCPM2 底座（内化于 vendor/ai_audio/voxcpm2） ───

# ─── ACE-Step 1.5 底座（内化于 vendor/ai_audio/ace_step） ───
